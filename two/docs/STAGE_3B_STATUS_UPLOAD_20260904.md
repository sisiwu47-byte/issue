# STAGE 3B STATUS

- Stage 3B 算法文件: `matlab/window_delta_velocity_indicator.m`
- 对应测试文件:
  - `tests/test_stage2_imu_track_and_fifo.m`（本次补强）
  - `tests/test_stage2_wss_candidate_and_window.m`（沿用既有场景）
- WINDOW INDEX AUDIT 结论:
  - `Ts = p.Ts_est = 0.01 s`
  - `Twindow = 0.5 s`
  - `Nwindow = 50`
  - `dvImuFIFO` 长度 = 50 个区间
  - `vxWheelFIFO` 长度 = 51 个时刻（51 列）
  - 实际窗口物理跨度 = `50 * Ts = 0.50 s`
- 时序结论:
  - 第50次有效100Hz调用：`windowReady=true`，但 `residualValid` 仍全为 `false`，`dVImu` 与 `eSlip` 尚非正式有效残差。
  - 第51次有效100Hz调用：`dVImu` 与 `DeltaVWheel` 首次正式有效，`residualValid` 全为 `true`，正式残差可用于滑移判定。
- 积分公式:
  - `dvImuStep = 0.5 * Ts_est * (axCorrPrev + axCorrCurrent)`
- 本次测试新增:
  - 已加入“变化加速度”场景，验证 `0.5*(1+3)*0.01 = 0.02` 的梯形积分贡献，且显式排除 `a_curr*Ts`、`a_prev*Ts` 两种欧拉形式的结果（通过数值阈值对比）。
  - 已加入50/51拍窗口边界断言：第50拍与第51拍行为差异明确。
  - 已加入 `windowReady` 与 `residualValid` 语义区分断言：`windowReady=true` 且 `residualValid=false` 时不应形成正式滑移残差。
- 现有 `reset` 与 `NaN` 处理结论:
  - `reset` 后窗口清空并可重新填充；
  - `NaN` IMU 输入会使 `imuLife=false`，并不应产生可靠 `dVImu`/`eSlip` 正式输出。
- MATLAB 验证状态: `PENDING MATLAB VALIDATION`
- BLOCKER: 0
- Stage 3B 最终状态:
  - `IMPLEMENTATION COMPLETE`
  - `TESTS WRITTEN`
  - `PENDING MATLAB EXECUTION`
- 下一阶段: `Stage 3C`
  - 连续可信度
  - 自适应单轮测量方差
  - 硬隔离
  - WSS 内部融合
